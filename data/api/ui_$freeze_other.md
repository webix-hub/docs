$freeze
======================

@short: blocks all component resizing

@example:

alert(webix.ui.$freeze);

@template:	api_config
@descr:

The property is intended for internal use by fullscreen module and rotation. 

We strongly encourage you not to change the value of the property. 
