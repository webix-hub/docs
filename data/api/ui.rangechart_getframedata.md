getFrameData
=============

@short:
	returns an array of objects that are included into the range

@params:


@returns:

- data			array 			 an array of objects that are included into the range (including the starting and ending ones)

@example:

rangechart.getFrameData();

@template:	api_method
@descr:

