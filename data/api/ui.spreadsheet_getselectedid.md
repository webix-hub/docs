getSelectedId
=============


@short:
	returns the ids of selected cells

@params:

- all

@returns:

- ids	the ids of the selected cells

@example:

$$("ss").getSelectedId(all);


@template:	api_method
@descr:

