updateItem
=============



@short:
	alias for refresh, repaints property view
    
@params:

@example:

var item = property.getItem("width");
item.value = "new one";
property.updateItem("width"); //same  as property.refresh();


@template:	api_method
@defined:	DataStore	
@descr:

Made for API compatibility with data components. 

