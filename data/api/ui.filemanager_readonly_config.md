readonly
=============

@short:
	sets File Manager into readonly mode ("false" by default)

@type: boolean
@example:
webix.ready(function(){
    webix.ui({
        view:"filemanager",
        readonly: true,
        url:"../common/data.php"
    });
});

@template:	api_config
@descr:


