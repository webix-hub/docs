showOption
=============


@short:
	shows a hidden option (segment) of the button

@params:

- id		string,number		the option id


@example:

// hide an option
segmented.hideOption(id);
// show the option again
segmented.showOption(id);

@template:	api_method
@descr:

@relatedapi:
api/ui.segmented_hideoption.md