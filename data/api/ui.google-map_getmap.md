getMap
=============

@short:
	returns a promise which is resolved after a map is rendered

@params:



@example:

$$("map").getMap().then(function(mapObj){
	// some code
});

@template:	api_method
@descr:

