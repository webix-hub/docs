getMap
=============

@short:
	returns a promise which is resolved after a map is rendered

@params:



@example:

$$("map").getMap().then(function(){
	// some code
});

@template:	api_method
@descr:

