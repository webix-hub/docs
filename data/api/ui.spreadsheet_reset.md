reset
=============

@short:
	resets the SpreadSheet settings to the initial state

@params:


@example:
$$("ss").config.columnCount = 5;
$$("ss").config.rowCount = 10;
$$("ss").reset();
		
		

@template:	api_method
@descr:

